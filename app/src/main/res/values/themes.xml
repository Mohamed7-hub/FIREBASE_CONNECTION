<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.FIREBASE_CONNECTION" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="colorPrimary">@color/teal</item>
    </style>

    <style name="Theme.FIREBASE_CONNECTION" parent="Base.Theme.FIREBASE_CONNECTION" />
</resources>